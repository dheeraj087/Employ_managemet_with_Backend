import { User } from "../models/user.model.js";
import { asyncHeandler } from "../utils/asyncHeandle.utils.js";
import { ErrorHeandle } from "../utils/ErrorHeandle.utils.js";

export const loginControllers = asyncHeandler((req, res, next) => {
  // const loginData = req.body;

  // if (!loginData || Object.keys(loginData) === 0) {
  //   throw new ErrorHeandle(403, ["data is not send"], "plz send the data ");
  // }


  
});
